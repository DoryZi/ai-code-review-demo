replicaCount: 1

image:
  repository: todo-app
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
